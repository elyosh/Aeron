#ifndef AERON_MAIN_H
#define AERON_MAIN_H

/*
 * Include this header exactly once, from the translation unit that defines
 * main(). SDL supplies the platform entry point and forwards it to main().
 */
#include <SDL3/SDL_main.h>

#endif
